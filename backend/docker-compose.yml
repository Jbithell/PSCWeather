version: "2.1"
services:
  serialConnection:
    build: ./serialConnection
    restart: unless-stopped
    privileged: true
    devices:
      - "/dev/ttyUSB0:/dev/ttyUSB0"
    labels:
      io.balena.features.supervisor-api: "1"
    environment:
      DATABASE: "/data/sqlite/data.sqlite"